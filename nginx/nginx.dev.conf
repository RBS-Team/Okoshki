# generated 2026-04-15, Mozilla Guideline v6.0, nginx 1.30.0, OpenSSL 3.6.1, intermediate config, HSTS, OCSP
# https://ssl-config.mozilla.org/#server=nginx&version=1.30.0&config=intermediate&openssl=3.6.1&hsts&ocsp&guideline=6.0

worker_processes auto;

events {
    worker_connections 1024;
}


http {
    include /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;

    gzip on;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;

    upstream core_backend {
        server core:8080;
    }

    # server {
    #     listen 443 ssl;
    #     listen [::]:443 ssl;
    #     http2 on;
    #     server_name okoski.ru www.okoski.ru;

    #     ssl_certificate /etc/letsencrypt/live/okoski.ru/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/okoski.ru/privkey.pem;

    #     # HSTS (ngx_http_headers_module is required) (63072000 seconds)
    #     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;

    #     # 1. НАСТРОЙКА ФРОНТЕНДА
    #     location / {
    #         root /usr/share/nginx/html;
    #         index index.html;
    #         try_files $uri $uri/ /index.html; # Нужно для работы роутинга React/Vue
    #         add_header Cache-Control "no-store, no-cache, must-revalidate";
    #     }

    #     location /api/v1 {
            
    #         proxy_pass http://core_backend; 
    #         client_max_body_size 10M;
            
    #         # Заголовки, чтобы бэкенд знал реальный IP юзера
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }

    # # intermediate configuration
    # ssl_protocols TLSv1.2 TLSv1.3;
    # ssl_ecdh_curve X25519MLKEM768:X25519:prime256v1:secp384r1;
    # ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305;
    # ssl_prefer_server_ciphers off;

    # # see also ssl_session_ticket_key alternative to stateful session cache
    # ssl_session_timeout 1d;
    # ssl_session_cache shared:MozSSL:10m;  # about 40000 sessions

    # # OCSP stapling
    # ssl_stapling on;
    # ssl_stapling_verify on;

    # # verify chain of trust of OCSP response using Root CA and Intermediate certs
    # ssl_trusted_certificate /etc/letsencrypt/live/okoski.ru/chain.pem;

    # # replace with the IP address of your resolver;
    # # async 'resolver' is important for proper operation of OCSP stapling
    # resolver 1.1.1.1 8.8.8.8 valid=300s;
    # resolver_timeout 5s;

    # # If certificates are marked OCSP Must-Staple, consider managing the
    # # OCSP stapling cache with an external script, e.g. certbot-ocsp-fetcher

    # HSTS
    server {
        listen 80 default_server;
        listen [::]:80 default_server;
        server_name localhost;    

        # 1. НАСТРОЙКА ФРОНТЕНДА
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html; # Нужно для работы роутинга React/Vue
            add_header Cache-Control "no-store, no-cache, must-revalidate";
        }

        location /api/v1 {
            
            proxy_pass http://core_backend; 
            client_max_body_size 10M;
            
            # Заголовки, чтобы бэкенд знал реальный IP юзера
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Нужно для Certbot, чтобы он мог подтвердить домен по HTTP
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # location / {
        # return 308 https://$host$request_uri;
        # }
    }
}